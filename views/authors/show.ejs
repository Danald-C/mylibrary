<h2 class="page-header"><%= authorObj.name %></h2>
<div class="btn-row">
    <a class="btn btn-primary" href="/authors/<%= authorObj.id %>/edit">Edit</a>
    <%- include('../partials/deleteForm', { url: `/authors/${authorObj.id}` }) %>
    <a class="btn btn-danger" href="/authors">&laquo; Back</a>
</div>
<% if(booksByAuthor.length > 0){ %>
    <h2 class="page-header">Books By Author</h2>
    <%- include('../partials/bookGrid', { books: booksByAuthor, bookGridClass: 'book-grid-3', bookCoverClass: 'book-cover-3' }) %>
<% } %>